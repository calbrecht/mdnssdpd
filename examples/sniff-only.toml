# Pure sniff/log mode — log all mDNS traffic as JSON to stdout
#
# Run: dnssd-powertools --config examples/sniff-only.toml

[[route]]
name = "sniff-all"
input = ["wlp0s20f3"]
output = [{ type = "log" }]
